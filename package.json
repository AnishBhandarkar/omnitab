{
  "scripts": {
    "build": "tsup",
    "build:worker": "tsup src/workers/omnitab-shared-worker.ts --format iife --out-dir dist/workers",
    "dev": "tsup --watch"
  },
  "devDependencies": {
    "@types/node": "^25.4.0",
    "tsup": "^8.5.1",
    "typescript": "^5.9.3"
  }
}
